# --- Application ------------------------------------------------------------
asgiref==3.12.1
Django==5.2.17
django-cors-headers==4.9.0
djangorestframework==3.18.1
djangorestframework_simplejwt==5.5.1
pillow==12.3.0
PyJWT==2.14.0
python-decouple==3.8
sqlparse==0.6.0

# --- Bases de données -------------------------------------------------------
# MySQL pour le poste de développement, PostgreSQL pour Render : le moteur est
# choisi au démarrage d'après DATABASE_URL, sans changer le code.
mysqlclient==2.3.0
psycopg[binary]==3.2.3
dj-database-url==2.3.0

# --- Service en production --------------------------------------------------
gunicorn==23.0.0
# Sert les fichiers statiques depuis le processus Django : Render n'a pas de
# serveur web séparé devant l'application.
whitenoise==6.8.2
